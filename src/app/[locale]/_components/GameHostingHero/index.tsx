'use client';

import React from 'react';
import HeroSection from './HeroSection';

const GameHostingHero = () => {
  return (
    <div className="w-full bg-[var(--color-primary)] dark:bg-[var(--color-primary)] text-[var(--color-text-primary)] dark:text-[var(--color-text-primary)]">
      <HeroSection />
    </div>
  );
};

export default GameHostingHero; 